{% extends 'base.html.twig' %}

{% block title %}Mon espace{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    {% for flash_success in app.flashes('success') %}
            <div class="alert alert-success" role="alert">{{ flash_success }}</div>
    {% endfor %}

    <h1>Bonjour {{ firstname }} !</h1>
    <br>
    {% if verified is same as true %}
    <div class="text"> Votre adresse email a été vérifiée ✅</div>
    {% elseif verified is same as false %}
    <div class="text">Votre adresse email n'a pas été vérifiée ❌</div>
    {% endif %}
    <h2>Vos données personnelles : </h2>
    <br><br>
    <p>{{ name }} {{ firstname }}</p>
    <p>{{ email }}</p>
    <br><br>
    {% if is_granted('ROLE_CUSTOMER') %}
        <h2>Votre {{ decription }}</h2>
        <br>
        <div class="text-center">
            <a class="btn btn-success" href="{{asset('article.zip')}}"> Article {{ Article }}</a>
        </div>
    {% endif %}

{# <div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href:"{{ 'C:/KIM/MCCI SIO/Stages/IFOX Code/Nouveau dossier (3)/e-commerce/src/Controller/MySpaceController.php'|file_link(0) }}">src/Controller/MySpaceController.php</a></code></li>
        <li>Your template at <code>C:/KIM/MCCI SIO/Stages/IFOX Code/Nouveau dossier (3)/e-commerce/templates/my_space/index.html.twig</code></li>
    </ul>
</div> #}
{% endblock %}
